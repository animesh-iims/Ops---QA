trial_4 (.xlsx) is a sample template of the file format supported by the Db tool
I5 is the latest iteration, preceded by I4
However, the indexed iteration is #3, which is the most stable and visibly functional
The PRD (.docx) is for I5, the latest iteration

author recommendation: updating patches on the currently indexed (I3) would be better (easier, faster, and more effective) than on the later iterations (I4, I5)
